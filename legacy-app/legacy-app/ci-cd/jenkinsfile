pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        sh 'docker build -t legacy-cloud-app .'
      }
    }
    stage('Deploy') {
      steps {
        sh 'echo Deploying to Cloud'
      }
    }
  }
}
